# To create CaCTus documentation locally you will need to install Sphinx

pip install sphinx
pip install sphinx-rtd-theme

# And then run

make html

# for local html website documentation or

make latexpdf # for PDF

# Which creates a sphinx based documentation in the docs/build folder.
